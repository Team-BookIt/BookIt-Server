<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            border: 2px dashed #FFC107;
        }
        .ticket {
            padding: 20px;
            max-width: 500px;
            margin: auto;
            text-align: left;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .ticket h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #FFC107;
        }
        .ticket p {
            margin: 5px 0;
            font-size: 18px;
        }
        .attendee-details {
            text-align: left;
        }
        .bookit-logo {
            padding: 20px;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="bookit-logo">
            <img src="<%= image_URL %>" alt="bookit-logo" width="240px" height="100px">
            <p>Thank you for booking with us!</p>
        </div>
        <div class="attendee-details">
            <h1>Event Ticket</h1>
            <p><strong>Name:</strong> <%= first_name %> <%= last_name%></p>
            <p><strong>Event:</strong> <%= event_name %></p>
            <p><strong>Booking ID:</strong> <%= booking_id %></p>
        </div>
    </div>
</body>
</html>
